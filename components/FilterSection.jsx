"use client";
import React from "react";

const FilterSection = ({ data, setFilteredData }) => {
  const filterDataFunc = (type, data) => {
    if (type === categories) {
    }
  };

  return <div></div>;
};

export default FilterSection;
